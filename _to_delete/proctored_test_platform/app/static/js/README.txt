Put onnxruntime-web's ort.min.js (and its .wasm files) here to enable browser-side phone
detection. Without it the page loads normally and reports the detector as unavailable.

Gitignored because of size - see docs/DEPLOYMENT.md.
